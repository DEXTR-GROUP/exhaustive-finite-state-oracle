# EFSO E4-001 — Exhaustive Enumerator Evidence

**Статус:** PASS
**Контрольная точка:** E4-001

## 1. Вопрос

Может ли EFSO исчерпывающе перечислить явно заданное конечное пространство без пропусков и дубликатов, с детерминированным порядком и воспроизводимым результатом?

## 2. Конечное пространство

`E4-BASE-3x3`

```text
a ∈ {-1, 0, 1}
b ∈ {-1, 0, 1}
|F| = 3 × 3 = 9
```

Каноническая идентичность состояния задаётся `state_id` из `src/efso_fm03.py`.

## 3. Проверяемые свойства

- `observed_count == expected_count`;
- `unique_count == expected_count`;
- `duplicate_count == 0`;
- `missing_count == 0`;
- детерминированный порядок повторного перечисления;
- точная воспроизводимость enumeration;
- стабильный SHA-256 digest последовательности state IDs.

## 4. Реализация

Основной enumerator:

```text
src/efso_exhaustive.py
```

Тесты:

```text
tests/test_exhaustive.py
```

Machine-readable evidence generator:

```text
validation/e4_001_evidence.py
```

CI workflow дополнительно формирует `evidence/e4_001.json` и сохраняет его как artifact `efso-e4-001-evidence`.

## 5. Фактическое evidence

CI run: **#9**  
Run ID: `35044801336`  
Commit: `267bdff265fd3f713bccdb06db9285c4cc25e89e`  
Job: `python-tests` / `104632211489`

Результат:

```text
pytest: 9 passed
E4 evidence generation: success
artifact upload: success
workflow conclusion: success
```

Artifact:

```text
efso-e4-001-evidence
artifact id: 10425614571
sha256: 95d6a03571bd69dbeea4b977cf370d8812abe520d14b68e11a9ec07b3c100411
```

Ожидаемая и наблюдаемая мощность: `9`.  
Уникальных состояний: `9`.  
Дубликатов: `0`.  
Пропусков: `0`.  
Детерминированный порядок: `true`.  
Воспроизводимый replay: `true`.  
Enumeration digest:

```text
389351c7d542fa8db7b00041f17fa285629230eff1e8a6c70f8ddaab3baba3df
```

## 6. Решение

Условие закрытия выполнено: фактический CI run существует, все тесты проходят, machine-readable evidence сформировано и artifact сохранён.

```text
E4-001 = PASS
```

## 7. Ограничение

E4-001 подтверждает свойства exhaustive enumeration на пространстве `E4-BASE-3x3`. Он не является numerical conformance test и не доказывает соответствие R4/R5/R6/R7 или IUT.
