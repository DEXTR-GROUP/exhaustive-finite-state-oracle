# FM2-R5-001 — Конечная модель R5

**Статус:** PASS  
**Контрольная точка:** FM2-R5-001

## 1. Цель

Зафиксировать явно определённое конечное пространство для независимой квалификации gated MLP до численного сравнения.

## 2. Семантика

Квалифицируется минимальная gated MLP-композиция:

```text
input[1]
  ├── gate linear → SiLU
  └── up linear
          ↓
       elementwise product
          ↓
       down linear
          ↓
       output[1]
```

Bias в текущий qualification space не включён, поскольку независимый QWENRNS R5 witness использует только веса и входной вектор.

## 3. Конечное пространство

```text
input        : 1 scalar
gate_weight  : 2 scalars
up_weight    : 2 scalars
down_weight  : 2 scalars
--------------------------------
total        : 7 scalars
```

Общий домен каждого скаляра:

```text
{-1, 0, 1}
```

Следовательно:

```text
|F_R5| = 3^7 = 2187
```

## 4. Фактический результат

CI run `35052989511`, commit `b665bc99361fd83de3a00de05fb3849ecc5cd90b`, job `104657174952` завершён успешно.

Machine-readable evidence `fm2_r5_001.json` создана. Structural qualification прошла до numerical conformance.

```text
observed_count           = 2187
unique_state_ids         = 2187
deterministic enumeration = true
canonical identity        = deterministic
machine-readable evidence = PASS
CI                         = PASS
```

## 5. Условие закрытия

```text
observed_count == 2187
unique_state_ids == 2187
deterministic enumeration == true
canonical identity == deterministic
machine-readable evidence generated
CI PASS
```

Все условия выполнены.

## 6. Ограничение

Это qualification space, а не утверждение об exhaustive coverage полного Qwen MLP. Большие пространства намеренно не выдаются за исчерпывающие.
