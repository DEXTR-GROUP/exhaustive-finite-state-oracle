# EFSO — МАЯК

**Идентификатор:** EFSO-MAIAK-001  
**Статус:** КАНОНИЧЕСКИЙ  
**Тип:** НОРМАТИВНАЯ ОПЕРАЦИОННАЯ МОДЕЛЬ

## 1. Назначение

МАЯК является единственной оперативной точкой ориентации проекта. Он фиксирует фактическое состояние, текущую рабочую границу, блокировки и одну ближайшую проверяемую контрольную точку.

МАЯК не является дорожной картой, спецификацией, журналом истории или доказательством сам по себе.

## 2. Основной принцип

```text
фактическое состояние
        ↓
текущая контрольная точка
        ↓
проверка
        ↓
закрытие / блокировка
        ↓
следующая контрольная точка
```

Непроверенное изменение не становится новым подтверждённым состоянием.

## 3. Что фиксируется

МАЯК должен содержать:

- текущий статус;
- текущую рабочую границу;
- последнее подтверждённое состояние;
- незавершённые вопросы;
- блокирующие условия;
- одну ближайшую контрольную точку;
- условия её закрытия;
- последний существенный результат;
- следующий обязательный шаг.

## 4. Статусы

Допустимы:

```text
ПЛАН
В РАБОТЕ
НА ПРОВЕРКЕ
ЗАБЛОКИРОВАНО
ПОВТОРНАЯ ПРОВЕРКА
ГОТОВО
```

Статус выбирается по evidence, а не по намерению.

## 5. Правила движения

1. Нет проверки — нет закрытия.
2. Нет подтверждения — нет нового базового состояния.
3. У проекта одна основная ближайшая контрольная точка.
4. Блокировка имеет приоритет над последующими этапами.
5. Неудача оставляет контрольную точку незакрытой.
6. Существенный новый дефект отражается в МАЯКе.
7. История не переписывается.
8. Статус не может быть сильнее имеющегося evidence.
9. После фактического PASS текущая контрольная точка закрывается до начала следующей.
10. Переход к следующей контрольной точке допускается только после синхронизации `EXPERIMENTS.md`, `МАЯК.md`, `CURRENT_STATE.md` и `QUALIFICATION_NEXT.md` с подтверждённым evidence.

## 6. Текущая контрольная точка

```text
Идентификатор: R7-NUM-001
Цель: выполнить независимую exhaustive numerical qualification для R7
Действие: определить корректное конечное R7 qualification space, source-exact external-origin witness и провести state-by-state comparison
Проверка: полное заявленное R7 finite space + external outputs + заранее фиксированная tolerance + machine-readable evidence
Условие закрытия: structural qualification PASS, numerical CI PASS, mismatch_count == 0, evidence и artifact присутствуют
Evidence: FM2-R6-001 PASS; R6-NUM-001 PASS; R5-NUM-001 PASS; E12-001 PASS
Результат: NEXT
```

## 7. Последний существенный результат

R6 contour закрыт: `FM2-R6-001 = PASS`, `R6-NUM-001 = PASS`. Полностью проверено `6561` состояний и `6561` external outputs; `mismatch_count = 0` при tolerance `1e-12`. Pinned external-origin witness прошёл source provenance verification.

R5 contour также закрыт: `FM2-R5-001 = PASS`, `R5-NUM-001 = PASS`, `2187` состояний и `2187` external outputs, `mismatch_count = 0` при tolerance `1e-12`.

## 8. Текущее состояние EFSO

Закрыты:

```text
FM0-FM3-001 = PASS
FM2-R4-001 = PASS  (исторический structural placeholder scope)
FM2-R4-002 = PASS
E4-001       = PASS
R4-NUM-001   = PASS
E5-001       = PASS
E6-001       = PASS
E7-001       = PASS
E8-001       = PASS
E9-001       = PASS
E10-001      = PASS
E11-001      = PASS
E12-001      = PASS
FM2-R5-001   = PASS
R5-NUM-001   = PASS
FM2-R6-001   = PASS
R6-NUM-001   = PASS
```

В работе:

```text
R7-NUM-001 = NEXT
```

Остаются незакрытыми:

```text
R7-NUM-001
NumPy float64 qualification
```

## 9. Синхронизация с дорожной картой

Каноническая дорожная карта реализации находится в `REDMI.md`. Она определяет порядок qualification stages, но не заменяет фактический статус evidence.

```text
REDMI.md
   ↓ задаёт порядок
EXPERIMENTS.md
   ↓ фиксирует статус проверки
CI / evidence
   ↓ подтверждает результат
МАЯК.md
   ↓ фиксирует текущую контрольную точку
CURRENT_STATE.md
   ↓ фиксирует подтверждённое состояние
QUALIFICATION_NEXT.md
   ↓ фиксирует следующий обязательный этап
```

Запрещено начинать следующую qualification stage только на основании наличия её раздела в `REDMI.md`. Сначала закрывается предыдущая контрольная точка и синхронизируются оперативные документы.

## 10. Перенос между диалогами

Диалог не является источником истины. Новый рабочий контекст восстанавливается в порядке:

```text
МАЯК
 ↓
main / commit
 ↓
CI / evidence
 ↓
нормативные документы
 ↓
следующий шаг
```

При конфликте приоритет имеет фактическое состояние репозитория и воспроизводимое evidence.

## 11. Инварианты

МАЯК должен сохранять:

- фактическое состояние вместо предположения;
- видимую блокировку;
- одну основную ближайшую контрольную точку;
- связь с evidence;
- непротиворечивость с `main`;
- синхронизацию с реестром проверок и текущим состоянием.

Изменение модели МАЯКА допускается только как отдельное нормативное изменение, если практика выявила универсальную проблему модели.
