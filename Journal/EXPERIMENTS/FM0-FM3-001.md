# EFSO — FM0–FM3 Qualification Run 001

**ID:** FM0-FM3-001  
**Статус:** PASS  
**Назначение:** первый исполняемый qualification contour EFSO без зависимости от IUT или внешнего numerical oracle.

## 1. Verification question

Может ли минимальный исполняемый контур EFSO независимо и воспроизводимо определить конечное пространство, каноническую идентичность состояний, точную cardinality и замкнутость переходов?

## 2. Реализация

Файл:

```text
src/efso_fm03.py
```

Тесты:

```text
tests/test_fm03.py
```

## 3. FM0 — Formal Boundary

Определены:

```text
State
Input
Parameter
Operation
Transition
Output
Invariant
FiniteStateSpace
Equality
Failure
Witness
```

Граница qualification отделяет structural proof EFSO от внешних numerical references.

## 4. FM1 — Independence

Контур не импортирует и не вызывает IUT или external oracle.

Проверяемые признаки:

```text
imports_iut = false
imports_external_oracle = false
calls_iut = false
calls_external_oracle = false
```

## 5. FM2 — Finite-State Model

Использовано минимальное пространство:

```text
A = {-1, 0, 1}
B = {-1, 0, 1}
F = A × B
|F| = 9
```

Enumeration определён напрямую через декартово произведение доменов.

## 6. FM3 — Exact Structural Model

Проверяются:

```text
observed_count == expected_count
unique_count == expected_count
missing_count == 0
duplicate_count == 0
canonical identity stable under serialization round-trip
transition closure
```

Каноническая identity основана на детерминированном JSON-представлении и SHA-256.

## 7. Controls

Тесты дополнительно проверяют:

```text
FM0-FM3 overall PASS
expected cardinality = 9
observed cardinality = 9
unique cardinality = 9
missing = 0
duplicates = 0
state identity stability
transition closure
```

## 8. Limitations

Этот run не квалифицирует numerical semantics R4–R7, NumPy float64 или реальный IUT. Он также не является полным доказательством универсальности EFSO.

Это первый structural baseline, на который далее накладываются exhaustive enumeration и external qualification witnesses.

## 9. Next decision

Следующий обязательный шаг:

```text
FM0-FM3
    ↓
Qualification adapter model
    ↓
R4 external reference integration
    ↓
exhaustive qualification space
    ↓
E4 Exhaustive Enumerator
```
