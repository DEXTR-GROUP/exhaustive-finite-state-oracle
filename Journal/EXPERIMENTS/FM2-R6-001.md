# FM2-R6-001 — Конечная модель R6

**Статус:** PASS  
**Контрольная точка:** FM2-R6-001

## 1. Цель

Зафиксировать явно определённое конечное пространство для независимой квалификации минимального Transformer Block до numerical comparison.

## 2. Reference semantics

Внешний R6 reference использует два токена, `hidden_size = 2`, `intermediate_size = 2`, RMSNorm, SiLU и residual composition.

```text
input
  ↓
RMSNorm (проверка конечности)
  ↓
residual = input + attention_update
  ↓
RMSNorm
  ↓
SiLU(x) * x
  ↓
residual + gated output
```

`epsilon = 1e-6` фиксирован.

## 3. Конечное пространство

```text
input             : 4 scalars
attention_update  : 4 scalars
--------------------------------
total             : 8 scalars
```

Общий домен каждого скаляра:

```text
{-1, 0, 1}
```

Следовательно:

```text
|F_R6| = 3^8 = 6561
```

## 4. Фактическое evidence

CI run `35053357639` на commit `18b1fce09c5cef3aef4b8f3c08f7efa119fccef2` завершён успешно.

Выполнены:

```text
observed_count == 6561
unique_state_ids == 6561
deterministic enumeration == true
canonical identity == deterministic
machine-readable evidence generated
CI PASS
```

## 5. Ограничение

Это qualification space, а не утверждение об exhaustive coverage полного Transformer Block большой модели.

## 6. Закрытие

`FM2-R6-001 = PASS` подтверждён фактическим CI execution. Машиночитаемое evidence `fm2_r6_001.json` вошло в artifact `efso-qualification-evidence`.
